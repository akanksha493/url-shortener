<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Document</title>
</head>
<body>
    <div class="container">
        <h1>URL Shrinker</h1>
        <form class="form-inline" action="/shortUrls" method="POST" >
            <div class="row">
                <label for="fullUrl" class="visually-hidden">Url</label>
                <input required placeholder="URL" class="form-control col" type="url" name="fullUrl" id="fullUrl">
                <button class="btn btn-success col-auto" type="submit">Shrink</button>
            </div>
            
        </form>
        <table class="table table-striped table-responsive">
            <thead>
                <tr>
                    <th>Full URL</th>
                    <th>Short URL</th>
                    <th>Click Count</th>
                </tr>
            </thead>
            <tbody>
                <% for(let i=0; i<data.length;i++){ %>
                <tr>
                    <td>
                        <a href="<%= data[i].fullUrl %>">
                            <%= data[i].fullUrl %>
                        </a>
                    </td>
                    <td>
                        <a href="<%= data[i].shortUrl %>">
                            <%= data[i].shortUrl %>
                        </a>
                    </td>
                    <td>
                        <%= data[i].clickCount %>
                    </td>
                </tr>
                <% } %>
            </tbody>
        </table>
    </div>
    
</body>
</html>